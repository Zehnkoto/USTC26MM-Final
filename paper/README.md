# Paper Source

This folder keeps the current LaTeX source and selected render-strip figures
referenced by `main.tex`.

Excluded: compiled PDF, LaTeX auxiliary files, raw trace CSV/JSON packages,
motion packages, and large render output directories.
