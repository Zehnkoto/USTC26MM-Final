# Experiment Notes

The repository keeps only source code and small report assets. Re-run outputs
should be written outside the repository or into ignored directories.

Key experiment families:

- interactive SuperSplat + PhysGaussian physical simulation;
- explicit / implicit / PBMPM solver ablations;
- generated ficus COLMAP matching-graph comparison;
- RoMa/SfM/MVS/3DGS initialization comparison.
